#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	char msg[] = "Programming is like building a multilingual puzzle";
  
	putchar('"');
	puts(msg);

	return (0);
}
